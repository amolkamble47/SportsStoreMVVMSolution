<UserControl x:Class="HookupSportsStoreWpfApp.Products.CatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HookupSportsStoreWpfApp.Products"
             xmlns:localBase="clr-namespace:HookupSportsStoreWpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             localBase:ViewModelLocator.AutoWireViewModel="True"
             >
    <Grid>
        <StackPanel>
            <Border BorderBrush="BurlyWood" BorderThickness="2" CornerRadius="5">
                <TextBlock Text="Cat View" TextAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="0, 3">
                <Border BorderBrush="DarkSlateBlue" BorderThickness="2" CornerRadius="5">
                    <TextBlock Text="{Binding CatDisplay}" TextAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
